## Packages
react-markdown | Safely render markdown content generated by the AI
date-fns | Formatting dates for article publication times

## Notes
The backend handles the AI generation when an article is requested via `/api/articles/:slug`. This may take 10-20 seconds, so the frontend relies heavily on visual loading states (skeletons) and does not timeout.
